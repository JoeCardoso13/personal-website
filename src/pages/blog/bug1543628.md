---
layout: ../../layouts/BlogPost.astro
title: "Bug 1543628: Rename Debugger image classes"
date: "December 11, 2025"
description: "How I completed a large-scale CSS refactoring to prevent styling conflicts, resolving an issue that had stalled multiple contributors."
---

> Patch history and review: [Phabricator D274481](https://phabricator.services.mozilla.com/D274481)

## Intro

In his presentation at FOSDEM 22, [Firefox Desktop Development 101](https://archive.fosdem.org/2022/schedule/event/mozilla_firefox_dev_101/), Mike Conley explains (at the 22:20 timestamp) that the platform used to render the contents of the webpages in the browser *is the same* platform that renders its UI. This means that the styling that you see on the [chrome](https://developer.mozilla.org/en-US/docs/Glossary/Chrome) (browser UI) of your Firefox is done with CSS, the logic that perform things like switching tabs is mostly written in JavaScript, and so forth. As a matter of fact, if you have the Mozilla Firefox browser installed, you can type `chrome://browser/content/browser.xhtml` in the address bar and you'll see Firefox's [chrome](https://developer.mozilla.org/en-US/docs/Glossary/Chrome) occupying the content display area!

This [bug](https://bugzilla.mozilla.org/show_bug.cgi?id=1543628) was circumscribed to one of the DevTools panels. Panels living in `devtools/client/`. Each panel has its own subdirectory. For this patch, we're working within `devtools/client/debugger/` - the JavaScript Debugger panel.

Inside `devtools/client/debugger/src/components/` you'll find React components that compose the Debugger's UI. These are organized into subdirectories reflecting the panel's layout:

  - `Editor/` - The source code viewer, tabs, and breakpoint gutters
  - `PrimaryPanes/` - The left sidebar containing the source tree and project search
  - `SecondaryPanes/` - The right sidebar with call stack, scopes, and watch expressions
  - `shared/` - Reusable components used across the Debugger

The `shared/` directory is particularly relevant to this patch - it contains **DebuggerImage**, a component that renders all the icons you see throughout the Debugger panel (folder icons in the source tree, play/pause buttons in the command bar, etc.).

As discussed previously, everything you see in the browser's UI - therefore every panel in DevTools: Console, Debugger, Netmonitor, etc - has to be stylized using CSS. And the file `devtools/client/debugger/src/debugger.css` serves as the main stylesheet entry point - it doesn't contain styles itself, but rather @import statements that pull in CSS from each component. When the Debugger panel loads, this single file orchestrates loading all the stylesheets needed to render the complete UI.

## The Problem

As the discussion in this [GitHub issue](https://github.com/firefox-devtools/debugger/issues/7905) shows, the Debugger's CSS class names are too generic. Names like `.img`, `.frame`, `.title`, and `.worker` existing in a global namespace shared by all DevTools panels is, as Florens put it, this is "the rough equivalent of writing JS like" this:

```js
window.img = function() { ... }
window.frame = function() { ... }
window.title = function() { ... }
window.worker = function() { ... }
```

This [bug](https://bugzilla.mozilla.org/show_bug.cgi?id=1543628) specifically is about a React component called `AccessibleImage` that was defined like this:
```js
  const AccessibleImage = props => {
    return React.createElement("span", {
      ...props,
      className: classnames("img", props.className),
    });
  };
```

The `classnames` is a utility function that combines CSS class names into a single string. So when you wrote `<AccessibleImage className="close" />` React rendered `<span class="img close"></span>`. The component always added `img` as a base class, then appended whatever you passed via `className`. The corresponding CSS would use these classes as selectors:
```cs
  .img {
    width: 16px;
    height: 16px;
    background-color: var(--theme-icon-color);
  }

  .img.close {
    mask-image: url(debugger/images/close.svg);
  }
```
Which is prone to cause cross-panel [namespacing](https://developer.mozilla.org/en-US/docs/Glossary/Namespace) conflicts.

## The Approach

For this patch, we're applying the BEM [namespacing](https://developer.mozilla.org/en-US/docs/Glossary/Namespace) strategy to the image component used throughout the Debugger panel. The core principle is simple: prefix everything with a **namespace** unique to the component or module. In this case, `dbg-` signals "Debugger panel." This makes collisions with other panels (Console, Network Monitor, Inspector) virtually impossible, even though all panels share the same global CSS namespace.

The implementation breaks down into three steps:

  1. Rename the `AccessibleImage` component to `DebuggerImage` and update its API to enforce namespaced class generation.
  2. Change the base class from the generic `img` to the namespaced `dbg-img`.
  3. Systematically rename all image-specific classes from `<name>` to `dbg-img-<name>`.

## Step-By-Step Implementation

### 1. Modifying the component

Rename the AccessibleImage component to just Image or DebuggerImage (it doesn't have any accessibility feature; we could add the ability to pass alt text, though.)

### 2. Changing the base class

Change the base class from img to something a bit less generic, for instance dbg-img.

### 3. Changing class names

Change all the image specific class names from <name> to dbg-img-<name>, for instance: dbg-img-worker.

## Adjusting CSS Specificity
